@{
    ViewData["Title"] = "Accesso Negato";
}

<div class="text-center mt-5">
    <h1 class="text-danger">
        <i class="bi bi-exclamation-triangle-fill"></i> Accesso Negato
    </h1>
    <p class="lead">Non disponi delle autorizzazioni necessarie per accedere a questa risorsa.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">Torna alla Dashboard</a>
</div>